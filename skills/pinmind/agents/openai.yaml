interface:
  display_name: "Pinmind"
  short_description: "Adaptive verified RU/EN task controller"
  default_prompt: "For non-trivial work, determine the route first, load only the needed specialist skills, and prove the result with current evidence."
policy:
  allow_implicit_invocation: true
