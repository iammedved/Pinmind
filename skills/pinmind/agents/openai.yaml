interface:
  display_name: "Pinmind"
  short_description: "Adaptive verified RU/EN task controller"
  default_prompt: "Use $pinmind для нетривиальной задачи: route it deterministically before task inspection, compose needed domain skills, follow the design/public-seam/root-cause/verify loop, and prove the result with current evidence."
policy:
  allow_implicit_invocation: true
